<div id="cn" style="display:none;">
            <%= render 'todos/test_todo_form'%>
</div>
<h1>Задачи</h1></div>
<a href='#' id="plusButton", onclick= "document.getElementById('cn').style.display = 'flex'">+</a>




     <% @projects.each do |project| %>
                    <h2><%= project.title%></h2>
                        <% project.todos.each do |todo| %>
                           <ul>
                               <li >
                                   <%= todo.text %>
                               </li>
                           </ul>
                        <%end%>
                    </div>
                </div>
            </div>
        <%end%>
    </div>
</div>
